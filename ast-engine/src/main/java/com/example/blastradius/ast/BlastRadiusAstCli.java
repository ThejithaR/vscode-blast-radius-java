package com.example.blastradius.ast;

// TODO(Member 3): main() — parse --workspace, --target, --target-package, --methods. Invoke ProjectScanner → TypeSolverBuilder → DependencyFinder → AstOutputBuilder. Print JSON to stdout.
public class BlastRadiusAstCli {
    public static void main(String[] args) {
        // TODO
    }
}
