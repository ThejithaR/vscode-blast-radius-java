{
  "name": "@blast-radius/ai-orchestrator",
  "version": "0.0.1",
  "private": true,
  "main": "dist/ai-orchestrator/src/index.js",
  "types": "dist/ai-orchestrator/src/index.d.ts",
  "scripts": {
    "build": "tsc -p ./tsconfig.json && node scripts/copy-prompts.mjs",
    "watch": "tsc -p ./tsconfig.json --watch",
    "test": "node --loader ts-node/esm --test src/**/*.spec.ts"
  },
  "dependencies": {
    "@blast-radius/shared": "*",
    "axios": "^1.6.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.3.0"
  }
}
