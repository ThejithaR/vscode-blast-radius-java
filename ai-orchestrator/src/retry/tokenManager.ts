// TODO(Member 4): estimate token count (rough: chars/4). If total prompt > model context, truncate each usageContextLine to N chars. Keep the dependency *count* intact; only individual lines shrink.
